const producto={
    nombre:"Monitor 20 pulgadas",
    precio:300,
    disponible:true,
   
}
//obtiene las llaves del objeto o los atribuots
console.log(Object.keys(producto))

console.log(Object.values(producto))

//retorna las llaves y los valores de las llaves
console.log(Object.entries(producto))