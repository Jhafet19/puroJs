

/*
import { suma } from '../js/funciones.js';


describe('Pruebas de suma', () => {
    test('Suma de 10 + 20 debe ser igual a 30', () => {
        expect(suma(10, 20)).toBe(30);
    });
    test('Suma de -1 + 1 debe ser igual a 0', () => {
        expect(suma(-1, 1)).toBe(0);
    });
});

*/